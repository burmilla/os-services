docker:
  image: ${REGISTRY_DOMAIN}/burmilla/os-k3s:1.32.3${SUFFIX}
  command: ros user-docker
  environment:
  - HTTP_PROXY
  - HTTPS_PROXY
  - NO_PROXY
  labels:
    io.rancher.os.scope: system
    io.rancher.os.after: console
  net: host
  pid: host
  ipc: host
  uts: host
  privileged: true
  restart: always
  volumes_from:
  - all-volumes
  volumes:
  - /etc/k3s:/etc/k3s
  - /sys:/host/sys
  - /var/lib/cni
  - /var/lib/kubelet
  - /var/lib/rancher/k3s
  - /var/lib/system-docker:/var/lib/system-docker:shared
