docker:
  image: ${REGISTRY_DOMAIN}/burmilla/os-docker:29.5.2${SUFFIX}
  command: ros user-docker
  environment:
  - DOCKER_MIN_API_VERSION=1.24
  - HTTP_PROXY
  - HTTPS_PROXY
  - NO_PROXY
  labels:
    io.rancher.os.scope: system
    io.rancher.os.after: console
  net: host
  pid: host
  ipc: host
  uts: host
  privileged: true
  restart: always
  volumes_from:
  - all-volumes
  volumes:
  - /sys:/host/sys
  - /var/lib/system-docker:/var/lib/system-docker:shared

